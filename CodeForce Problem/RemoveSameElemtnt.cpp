#include<iostream>
#include<string>
#include<vector>
using namespace std;
int main ()
{
    int n;
    n = 6;
    while(n--){
    int a,b;
    int a,b;
    string num = to_string(b);
    vector<char> v;
    for (int i=0;i<num.length();i++){
        if (num[i] == a ){
            continue;
        }
        v.push_back(n[i]);
    }
    }
return 0;
}